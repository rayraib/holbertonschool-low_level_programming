#include "holberton.h"
#include <stdio.h>
/**
* main - Prints its, the program's, name
* @argc: unused argument counter
* @argv: Array of argument strings
* Return: Succes 0
*/
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
